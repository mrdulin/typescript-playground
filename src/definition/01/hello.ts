/**
 * Created by dulin on 2017/6/29.
 */
export function getName(first: string, last: string): string {
  return first + ' ' + last;
}

export function sayHi(name: string): void {
  console.log(name);
}
